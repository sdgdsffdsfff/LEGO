package com.ecfront.lego.core.component.protocol

case class ResponseProtocol(
                             code: String,
                             message: String,
                             body: String)
